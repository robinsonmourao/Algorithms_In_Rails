<%= form_with(url: algorithms_returns_path, method: "get") do |form| %>
    <div class="field">
      <%= form.label :value , "Digite o valor:" %>
      <%= form.text_field :value %>
    </div>
    <div class="actions">
      <%= form.submit "Calculate" %>
    </div>
  <%= @palindromo %>
  <% end %>

<%= form_with(url: algorithms_returns_path, method: "get") do |form| %>
    <div class="field">
      <%= form.label :value %>
      <%= form.text_field :value %>
    </div>
    <div class="actions">
      <%= form.submit "Calculate" %>
    </div>
  <%= @maior_idade %>
  <% end %>